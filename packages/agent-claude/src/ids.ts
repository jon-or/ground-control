/** Named here rather than in `claude.ts` so the classifier can use it without importing the roster reader. */
export const CLAUDE_AGENT_ID = 'claude';
export const CLAUDE_DISPLAY_NAME = 'Claude Code';
