/** Named here rather than in `codex.ts` so the hook writer and the history reader use it without the roster reader. */
export const CODEX_AGENT_ID = 'codex';
export const CODEX_DISPLAY_NAME = 'Codex';
