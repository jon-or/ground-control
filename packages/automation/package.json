{
  "name": "@ground-control/automation",
  "version": "0.0.0",
  "private": true,
  "description": "Decides which cards the board may act on, and remembers what it ran. Must not import vscode.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@ground-control/core": "*",
    "zod": "^3.25.0"
  }
}
