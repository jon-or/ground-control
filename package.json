{
  "name": "ground-control-root",
  "private": true,
  "version": "0.0.0",
  "description": "Assigned GitHub issues and local Claude Code and Codex sessions.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/core",
    "packages/agent-claude",
    "packages/agent-codex",
    "packages/host-vscode",
    "packages/github",
    "packages/board",
    "packages/automation",
    "packages/hub",
    "apps/hub",
    "extensions/ground-control",
    "extensions/chrome-github-board"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "watch": "npm run watch --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "verify": "npm run build && npm run typecheck && npm test",
    "prepare": "git rev-parse --git-dir >/dev/null 2>&1 && git config core.hooksPath .githooks || exit 0",
    "hub": "node apps/hub/dist/main.js",
    "test:integration": "npm run build && npm run test:integration --workspace ground-control",
    "verify:full": "npm run verify && npm run test:integration"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.9.0"
  }
}
