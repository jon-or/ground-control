{
  // Disable js-debug network reporting. Other inspector clients can still enable it; stream handling must remain
  // compatible with inspector reporting (docs/mechanics.md M28).
  "debug.javascript.enableNetworkView": false
}
