{
  // js-debug asks a debuggee it launches to mirror network events. Narrows the exposure only: any connected
  // inspector frontend enables the same reporting, and what made it fatal is fixed in the streams (`docs/mechanics.md` §28).
  "debug.javascript.enableNetworkView": false
}
